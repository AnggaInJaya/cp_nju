<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portofolio - Prima Events</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Load Bootstrap 5 CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" xintegrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Load Lucide Icons CDN -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        /* Mengatur font kustom dan latar belakang */
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8f9fa; /* Warna latar belakang umum */
        }
        /* Gradient kustom untuk tombol CTA */
        .cta-gradient {
            background-image: linear-gradient(to right, #10b981, #06b6d4); /* Emerald ke Cyan */
            border: none;
            color: white;
            transition: all 0.3s ease;
        }
        .cta-gradient:hover {
            opacity: 0.9;
            transform: scale(1.02);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }
        /* Warna merek */
        .prima-text { color: #06b6d4; /* Cyan */ }
        .prima-brand { color: #10b981; /* Teal/Emerald */ }

        /* Gaya Khusus untuk Portofolio */
        .portfolio-item {
            background-color: white;
            border-radius: 1rem;
            overflow: hidden;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
        }
        .portfolio-item:hover {
            box-shadow: 0 1rem 2rem rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>

    <!-- Header & Navigasi (Sama dengan index.html) -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm">
            <div class="container-fluid container-lg">
                <a class="navbar-brand fs-4 fw-bolder" href="index.html">
                    <span class="prima-text">PRIMA</span> <span class="prima-brand">EVENTS</span>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item"><a class="nav-link" href="index.html">Beranda</a></li>
                        <li class="nav-item"><a class="nav-link" href="index.html#services">Layanan</a></li>
                        <li class="nav-item"><a class="nav-link active" aria-current="page" href="portfolio.html">Portofolio</a></li>
                        <li class="nav-item"><a class="nav-link" href="index.html#locations">Lokasi</a></li>
                        <li class="nav-item"><a class="nav-link" href="index.html#contact">Kontak</a></li>
                    </ul>
                    <a href="index.html#contact" class="btn btn-primary rounded-pill cta-gradient px-4 py-2 fw-semibold d-grid gap-2 d-md-block">
                        Dapatkan Penawaran Gratis
                    </a>
                </div>
            </div>
        </nav>
    </header>

    <main>
        <!-- Judul Halaman Portofolio -->
        <section class="py-5 bg-light">
            <div class="container container-lg text-center">
                <h1 class="display-4 fw-bold text-dark">Portofolio Acara Kami</h1>
                <p class="lead text-muted">Intip hasil kerja terbaik Prima Events dari Bandung hingga Bali. Setiap acara, sebuah cerita sukses.</p>
                <div class="d-flex justify-content-center gap-3 mt-4">
                    <button type="button" class="btn btn-outline-success btn-sm rounded-pill">Korporat</button>
                    <button type="button" class="btn btn-outline-info btn-sm rounded-pill">Pernikahan</button>
                    <button type="button" class="btn btn-outline-primary btn-sm rounded-pill">Pameran & Konferensi</button>
                </div>
            </div>
        </section>

        <!-- Daftar Proyek Portofolio -->
        <section id="portfolio-list" class="py-5">
            <div class="container container-lg">
                <div class="row g-5">
                    
                    <!-- Proyek 1: Peluncuran Produk (Korporat) -->
                    <% @services.each do |service| %>
                    <div class="col-lg-12">
                        <div class="portfolio-item row g-0">
                            <div class="col-lg-6">
                                <img src="<%= service.avatars.first.url %>" class="img-fluid" alt="">
                            </div>
                            <div class="col-lg-6 p-4 p-md-5">
                                <h2 class="h3 fw-bold <%= ['text-primary','text-secondary','text-success','text-danger','text-warning','text-info'].sample %>">
                                    <%= service.name %>
                                </h2>
                                <hr class="my-3">
                                <%= simple_format(service.description) %>
                            </div>
                        </div>
                    </div>
                    <% end %>

                </div>
            </div>
        </section>
        
        <!-- CTA mini di halaman portofolio -->
        <section class="py-4 bg-white border-top">
            <div class="container container-lg d-flex justify-content-between align-items-center flex-column flex-md-row">
                <h4 class="h5 fw-bold mb-3 mb-md-0">Tertarik? Mari Wujudkan Visi Anda.</h4>
                <a href="index.html#contact" class="btn cta-gradient rounded-pill px-4 fw-semibold">
                    Konsultasikan Acara Anda &rarr;
                </a>
            </div>
        </section>

    </main>

    <!-- Footer (Sama dengan index.html) -->
    <footer class="bg-dark text-white">
        <div class="container container-lg py-4">
            <div class="d-flex justify-content-center flex-wrap gap-3 mb-3">
                <a href="index.html#services" class="text-white-50 text-decoration-none small mx-2">Layanan</a>
                <a href="portfolio.html" class="text-white-50 text-decoration-none small mx-2">Portofolio</a>
                <a href="index.html#locations" class="text-white-50 text-decoration-none small mx-2">Lokasi</a>
                <a href="index.html#contact" class="text-white-50 text-decoration-none small mx-2">Kontak</a>
                <a href="#" class="text-white-50 text-decoration-none small mx-2">Karier</a>
            </div>
            <div class="d-flex justify-content-center gap-3 mb-3">
                <!-- Ikon Media Sosial -->
                <a href="#" class="text-secondary hover-white transition"><i data-lucide="instagram" style="width: 20px; height: 20px;"></i></a>
                <a href="#" class="text-secondary hover-white transition"><i data-lucide="facebook" style="width: 20px; height: 20px;"></i></a>
                <a href="#" class="text-secondary hover-white transition"><i data-lucide="linkedin" style="width: 20px; height: 20px;"></i></a>
            </div>
            <p class="text-center text-secondary small mb-0">
                &copy; 2025 Prima Events. Semua hak dilindungi. Kantor: Bandung, Jawa Barat, Indonesia.
            </p>
        </div>
    </footer>

    <!-- Bootstrap Bundle dengan Popper (WAJIB) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" xintegrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <!-- Inisialisasi Ikon Lucide -->
    <script>
        lucide.createIcons();
    </script>
</body>
</html>